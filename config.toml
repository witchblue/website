baseURL = ""

# Change the default theme to be use when building the site with Hugo
theme = "LoveIt"

# website title
title = "witchblue"
description = "heyy"

# language code ["en", "zh-CN", "fr", "pl", ...]
languageCode = "en"
# language name ["English", "简体中文", "Français", "Polski", ...]
languageName = "English"

# Author config
[author]
  name = "witchblue"
  email = "witchblue@proton.me"
  link = "mailto:witchblue@proton.me"

  [params.social]
  email= "mailto:witchblue@proton.me"
  Linkedin ="https://www.linkedin.com/in/shakeerunnisa-s-66b1b4233/"

  # Footer config
  [params.footer]
    enable = true
    # LoveIt NEW | 0.2.0 Custom content (HTML format is supported)
    custom = '"Stay Hungry, Stay Foolish" -Steve Job'
    # LoveIt NEW | 0.2.0 whether to show Hugo and theme info
    hugo = false
    # LoveIt NEW | 0.2.0 whether to show copyright info
    copyright = false
    # LoveIt NEW | 0.2.0 whether to show the author
    author = false
    # Site creation time
    #since = 2019
    # ICP info only in China (HTML format is supported)
    #icp = ""
    
    [params]
  # site default theme ["auto", "light", "dark"]
  defaultTheme = "dark"
  # website title for Open Graph and Twitter Cards
  title = "witchblue"
  # website description for RSS, SEO, Open Graph and Twitter Cards
  #description = "heyya!!"

  # Header config
  [params.header]
    # desktop header mode ["fixed", "normal", "auto"]
    desktopMode = "fixed"
    # mobile header mode ["fixed", "normal", "auto"]
    mobileMode = "auto"
    # LoveIt NEW | 0.2.0 Header title config
    [params.header.title]
      # URL of the LOGO
      logo = ""
      # title name
      name = "witchblue"
      # LoveIt NEW | 0.2.5 whether to use typeit animation for title name
      typeit = true

      # Home page config
  [params.home]
    # LoveIt NEW | 0.2.0 amount of RSS pages
    rss = 10
    # Home page profile
    [params.home.profile]
      enable = true
      # Gravatar Email for preferred avatar in home page
      #gravatarEmail = ""
      # URL of avatar shown in home page
      avatarURL = "/images/logo.png"
      # LoveIt CHANGED | 0.2.7 title shown in home page (HTML format is supported)
      title = "Hi"
      # subtitle shown in home page (HTML format is supported)
      subtitle = "welcome"
      # whether to use typeit animation for subtitle
      typeit = true
      # whether to show social links
      social = true
      # LoveIt NEW | 0.2.0 disclaimer (HTML format is supported)
      disclaimer = "Disclaimer: The information provided in this blog is intended for educational and informational purposes only. "
    # Home page posts
    [params.home.posts]
      enable = true
      # special amount of posts in each home posts page
      paginate = 6
      # LoveIt DELETED | 0.2.0 replaced with hiddenFromHomePage in params.page
      # default behavior when you don't set "hiddenFromHomePage" in front matter
      defaultHiddenFromHomePage = false

      [params.page.share]
      enable = false
      



# Menu config
[menu]
  [[menu.main]]
    weight = 1
    identifier = "posts"
    # you can add extra information before the name (HTML format is supported), such as icons
    pre = ""
    # you can add extra information after the name (HTML format is supported), such as icons
    post = ""
    name = "Posts"
    url = "/posts/"
    # title will be shown when you hover on this menu link
    title = ""
  [[menu.main]]
    weight = 2
    identifier = "about"
    pre = ""
    post = ""
    name = "About"
    url = "/about"
    title = ""
  [[menu.main]]
    weight = 3
    identifier = "categories"
    pre = ""
    post = ""
    name = "Categories"
    url = "/categories/"
    title = ""

# Markup related configuration in Hugo
[markup]
  # Syntax Highlighting (https://gohugo.io/content-management/syntax-highlighting)
  [markup.highlight]
    # false is a necessary configuration (https://github.com/dillonzq/LoveIt/issues/158)
    noClasses = false
baseURL = 'http://example.org/'
languageCode = 'en-us'
title = 'My New Hugo Site'
